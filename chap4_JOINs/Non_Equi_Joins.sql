--Non-Equi-Joins

/*
joins with comparison operator on WHERE or ON clause called theta joins,
where theta represents the relational operator.

Inner joins with an = operator called equi-joins
joins with an operator other than an = sign called non-equi-joins; non-equi-joins are rare
*/
